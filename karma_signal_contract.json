{
  "subject_id": {
    "type": "string",
    "description": "UUID of the subject"
  },
  "product_context": {
    "type": "string",
    "description": "Platform context (assistant | game | finance | gurukul | infra | workflow)"
  },
  "signal": {
    "type": "string",
    "description": "Type of signal (allow | nudge | restrict | escalate)",
    "enum": ["allow", "nudge", "restrict", "escalate"]
  },
  "severity": {
    "type": "number",
    "description": "Severity level (0.0 to 1.0)",
    "minimum": 0.0,
    "maximum": 1.0
  },
  "ttl": {
    "type": "number",
    "description": "Time to live in seconds"
  },
  "requires_core_ack": {
    "type": "boolean",
    "description": "Whether Core ACK is required before applying consequences",
    "default": true
  },
  "opaque_reason_code": {
    "type": "string",
    "description": "Opaque reason code for the signal (no human-readable explanations)"
  }
}